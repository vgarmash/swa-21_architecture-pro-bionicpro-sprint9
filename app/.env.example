# OAuth2 AES Key (32 characters for AES-256)
OAUTH2_AES_KEY=<generate-with: openssl rand -base64 24>

# OAuth2 Salt
OAUTH2_SALT=<generate-with: openssl rand -base64 16>

# Keycloak Client Secrets
KEYCLOAK_CLIENT_SECRET=mAu+Af98H0Bq5diUVwbU0ymE76Bqpx55
KEYCLOAK_BIONICPRO_AUTH_CLIENT_SECRET=<generate-with: openssl rand -base64 32>
